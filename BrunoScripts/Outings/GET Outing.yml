info:
  name: "GET Outing "
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/outings/0
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
