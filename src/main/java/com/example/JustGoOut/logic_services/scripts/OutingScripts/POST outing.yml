info:
  name: POST outing
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/outings
  body:
    type: json
    data: |-
      {
        "outingName": "Archery",
        "outingDesc": "Shooting arrows at the target"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
