info:
  name: GET incomplete outings
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/outings?status=INCOMPLETE
  params:
    - name: status
      value: INCOMPLETE
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
